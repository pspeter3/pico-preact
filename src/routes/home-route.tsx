import type { VNode } from "preact";

export function HomeRoute(): VNode {
  return (
    <header>
      <hgroup>
        <h1>Pico Preact</h1>
        <p>PicoCSS Preact Template</p>
      </hgroup>
    </header>
  );
}
